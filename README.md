# GeneralRNProject

## 1：配置 TypeScript 编译器

1. 安装ts依赖
```
yarn add tslib @types/react @types/react-native
yarn add --dev react-native-typescript-transformer typescript
```
2. 配置 tsconfig.json
3. 新建或编辑 rn-cli.config.js
